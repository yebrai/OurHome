<div>
  <a href="/property/create">Add house</a>
  <a href="/property/favourite">Favourites</a>

  <h1>List of houses</h1>
</div>

{{#each listProperties}}

  {{!-- <div>
      <div class="col-sm-6">
        <div class="card">
          <div class="card-body">
            <img src="{{this.img}}" alt="Img-property" />
            <h5 class="card-title">{{this.name}}</h5>
            <p class="card-text">Location: {{this.location}}.</p>
            <p class="card-text">Price: $ {{this.price}}.-</p>
            <p class="card-text">Property for {{this.apartmentFor}}.</p>
            <a href="#" class="btn btn-primary">Message!</a>
            <a href="#" class="btn btn-primary">Add Favourites</a>
          </div>
        </div>
      </div> --}}
  <div class="row row-cols-1 row-cols-md-2 g-4">
    <div class="col">
      <div class="card">
        <img src="{{this.img}}" class="card-img-top" alt="img-property" />
        <div class="card-body">
          <h5 class="card-title">{{this.name}}</h5>
          <p class="card-text">Location: {{this.location}}.</p>
          <p class="card-text">Price: $ {{this.price}}.-</p>
          <p class="card-text">Property for {{this.apartmentFor}}.</p>

          <a href="/property/details/{{this._id}}" class="btn btn-outline-info">More details!</a>
          <form action="/property/{{this._id}}/favourite" method="post">
            <a href="#" class="btn btn-primary">Add Favourites</a>
          </form>
          {{#if isUserActive}}
        {{#if isProfessionalActive}}
          <a href="/professional/profile">Profile</a>
          <a href="/professional/list">Houses</a>
        {{else}}
          <a href="/profile">Profile</a>
          <a href="/property/list">Houses</a>
        {{/if}}
        <a href="/auth/logout">Close</a>
      {{else}}
        <a href="/">Home</a>
        <div class="acces-nav">
          <a href="/auth/login">Log in</a>
          <a href="/auth/signup">Sign up</a>
        </div>
      {{/if}}

        </div>
      </div>
    </div>
  </div>

{{/each}}